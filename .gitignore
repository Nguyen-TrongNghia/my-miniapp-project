
# Thư viện phụ thuộc tải về (rất nặng, không cần đẩy lên GitHub)
node_modules/
jspm_packages/

# File cấu hình môi trường chứa thông tin bảo mật/mật khẩu
.env
.env.local
.env.development.local
.env.production.local

# Thư mục chứa bản build sau khi đóng gói ứng dụng
dist/
build/
out/

# Các file rác do hệ điều hành tự động sinh ra
.DS_Store
Thumbs.db
*.log
